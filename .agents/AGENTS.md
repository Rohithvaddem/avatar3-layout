# Workspace Rules

- **Automatic GitHub Sync**: After making any code changes or completing user requests, automatically commit and push the changes to GitHub (`origin main`) so the live repository is always up to date.
